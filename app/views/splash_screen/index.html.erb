<section class="d-flex flex-column align-items-center justify-content-center" style="height:100vh;">
  <div>
    <h1>Budget App</h1>
  </div>
  <div class="d-flex flex-column gap-4 mt-5">
    <% if user_signed_in?%>
      <div>
        <%= link_to "Categories", categories_path, class: "btn btn-primary w-100
" %>
      <%else%>
      </div>
      <div class="mb-4">
        <%= link_to "LOG IN", new_user_session_path, class:"btn btn-primary w-100" %>
      </div>
      <div>
        <%= link_to "SIGN UP", new_user_registration_path, class:"btn btn-primary w-100" %>
      </div>
    <%end%>
  </div>
</section>
